#include <stdio.h>

int main () {
    printf("202533713 김진서\n");

    int N, M, dan;
    int minSpace, avgSpace;

    printf("N: ");
    scanf("%d", &N);
    printf("M: ");
    scanf("%d", &N);

    dan = M - N + 1;
    minSpace = 80 - (dan * 10) + dan + 1;
    avgSpace = (80 - (dan * 10)) / (dan + 1);

    for ();
}